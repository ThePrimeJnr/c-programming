#include <stdio.h>

/**
 * main - Calculates the area of a circle given it's radius
 *
 * Return: Always 0 (Success)
 *
 * Destiny Saturday (DestinedCodes)
 * 10-05-2023
*/

int main(void)
{
	float radius;

	printf("Enter Circle's radius: ");
	scanf("%f", &radius);
	printf("The radius is %f\n", radius);

	return (0);
}
